# PostgreSQL (Neon / Supabase / local Docker). Vercel: paste the provider connection string here.
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require"

# Currency symbol for store UI
NEXT_PUBLIC_CURRENCY_SYMBOL="$"
